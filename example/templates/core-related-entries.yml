kind: html
group: template
version: v1
namespace: core
name: related-entries
spec:
  body: |-
    {{ range $item := . }}
      <li>
        <h3><a href="{{ $item.Href }}">{{ $item.Spec.Title }}</a></h3>
        <p class="content">{{ $item.Spec.Description }}</p>
      </li>
    {{ end }}