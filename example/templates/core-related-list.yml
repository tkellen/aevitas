kind: html
group: template
version: v1
namespace: core
name: related-list
spec:
  body: |-
    {{ range $item := . }}
      <li><a href="{{ $item.Href }}">{{ $item.Spec.Title }}</a></li>
    {{ end }}