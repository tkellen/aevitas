kind: html
group: template
version: v1
namespace: core
name: layout
meta:
  include:
  - resource: html/template/v1/core/head
    as: head
spec:
  body: |-
    <!DOCTYPE html>
    <html>
      {{ template "head" . }}
      <body>
      {{ yield }}
      </body>
    </html>