kind: html
group: template
version: v1
namespace: image
name: layout
meta:
  live: true
render:
  imports:
  - name: image
    selector: html/template/v1/default/responsive-image
body: |-
  {{ image . }}
  {{ .Meta.Description }}
